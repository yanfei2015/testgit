<?xml version="1.0" encoding="UTF-8" ?>
<!--
  ~ Copyright (c) 2014 杭州端点网络科技有限公司
  -->

<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >

<mapper namespace="ItemDetail">
    <resultMap id="ItemDetailMap" type="ItemDetail">
        <id column="id" property="id"/>
        <result column="item_id" property="itemId"/>
        <result column="image1" property="image1"/>
        <result column="image2" property="image2"/>
        <result column="image3" property="image3"/>
        <result column="image4" property="image4"/>
        <result column="packing" property="packing"/>
        <result column="service" property="service"/>
        <result column="attr_images" property="attrImages"/>
        <result column="created_at" property="createdAt"/>
        <result column="updated_at" property="updatedAt"/>
    </resultMap>

    <sql id="tb">
        parana_item_details
    </sql>

    <sql id="cols_all">
        id, <include refid="cols_exclude_id" />
    </sql>

    <sql id="cols_exclude_id">
        item_id, image1, image2, image3, image4, packing, service, attr_images, created_at, updated_at
    </sql>

    <sql id="vals">
        #{itemId}, #{image1}, #{image2}, #{image3}, #{image4}, #{packing}, #{service}, #{attrImages}, now(), now()
    </sql>

    <sql id="criterias">
        WHERE 1 = 1
        <if test="itemId != null"> AND item_id = #{itemId} </if>
    </sql>

    <insert id="create" parameterType="ItemDetail" keyProperty="id" useGeneratedKeys="true">
        INSERT INTO
        <include refid="tb" />
        (<include refid="cols_exclude_id" />)
        VALUES
        (<include refid="vals" />)
    </insert>

    <select id="load" parameterType="long" resultMap="ItemDetailMap" >
        SELECT <include refid="cols_all" />
        FROM <include refid="tb" />
        WHERE id = #{id} LIMIT 1
    </select>

    <select id="findByItemId" parameterType="long" resultMap="ItemDetailMap" >
        SELECT <include refid="cols_all" />
        FROM <include refid="tb" />
        WHERE item_id = #{itemId} LIMIT 1
    </select>

    <select id="list" parameterType="ItemDetail" resultMap="ItemDetailMap">
        SELECT id,
        <include refid="cols_all"/>
        FROM
        <include refid="tb"/>
        <include refid="criterias"/>
    </select>

    <update id="update" parameterType="ItemDetail">
        UPDATE <include refid="tb"/>
        <set>
            <if test="image1 != null"> image1 = #{image1}, </if>
            <if test="image2 != null"> image2 = #{image2}, </if>
            <if test="image3 != null"> image3 = #{image3}, </if>
            <if test="image4 != null"> image4 = #{image4}, </if>
            <if test="packing != null"> packing = #{packing}, </if>
            <if test="service != null"> service = #{service}, </if>
            <if test="attrImages != null"> attr_images = #{attrImages}, </if>
            updated_at=now()
        </set>
        WHERE id=#{id}
    </update>

    <delete id="delete" parameterType="long">
        DELETE FROM <include refid="tb"/>
        WHERE id = #{id}
    </delete>

    <delete id="deleteByItemId" parameterType="long">
        DELETE FROM <include refid="tb"/>
        WHERE item_id = #{itemId}
    </delete>

    <!-- 计数 -->
    <select id="count" parameterType="map" resultType="long">
        SELECT COUNT(1)
        FROM <include refid="tb"/>
        <include refid="criterias"/>
    </select>

    <!-- 分页查询 -->
    <select id="paging" parameterType="map" resultMap="ItemDetailMap">
        SELECT
        <include refid="cols_all"/>
        FROM <include refid="tb"/>
        <include refid="criterias"/>
        LIMIT #{offset}, #{limit}
    </select>

</mapper>