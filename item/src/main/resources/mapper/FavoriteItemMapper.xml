<?xml version="1.0" encoding="UTF-8" ?>
<!--
  ~ Copyright (c) 2014 杭州端点网络科技有限公司
  -->

<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >

<mapper namespace="FavoriteItem">
    <resultMap id="FavoriteItemMap" type="FavoriteItem">
        <id column="id" property="id"/>
        <result column="item_id" property="itemId"/>
        <result column="user_id" property="userId"/>
        <result column="brand" property="brand"/>
        <result column="brand_picture" property="brandPicture"/>
        <result column="name" property="name"/>
        <result column="image" property="image"/>
        <result column="price" property="price"/>
        <result column="origin_price" property="originPrice"/>
        <result column="discount" property="discount"/>
        <result column="paid_number" property="paidNumber"/>
        <result column="created_at" property="createdAt"/>
        <result column="updated_at" property="updatedAt"/>
    </resultMap>

    <sql id="tb">
        parana_favorite_items
    </sql>

    <sql id="cols_all">
        id, <include refid="cols_exclude_id" />
    </sql>

    <sql id="cols_exclude_id">
        item_id,user_id, brand, brand_picture, `name`, image, price, origin_price, discount, paid_number, created_at, updated_at
    </sql>

    <sql id="vals">
        #{itemId}, #{userId}, #{brand}, #{brandPicture}, #{name}, #{image}, #{price}, #{originPrice}, #{discount}, #{paidNumber}, now(), now()
    </sql>


    <sql id="criteria">
        WHERE 1 = 1
        <if test="id != null"> AND id = #{id} </if>
        <if test="userId != null"> AND user_id = #{userId}</if>
        <if test="name != null">AND name LIKE CONCAT('%', #{name} ,'%') </if>
        <if test="itemId != null"> AND item_id = #{itemId}</if>
        <if test="brand != null" > AND brand = #{brand}</if>
        <if test="updatedFrom != null"> AND <![CDATA[updated_at >= #{updatedFrom}]]> </if>
        <if test="updatedTo != null"> AND <![CDATA[updated_at < #{updatedTo}]]> </if>
        <if test="itemIds != null">
            AND item_id in
            <foreach collection="itemIds" item="itemId" separator="," open="(" close=")">
                #{itemId}
            </foreach>
        </if>
    </sql>

    <insert id="create" parameterType="FavoriteItem" keyProperty="id" useGeneratedKeys="true">
        INSERT INTO
        <include refid="tb" />
        (<include refid="cols_exclude_id" />)
        VALUES
        (<include refid="vals" />)
    </insert>

    <select id="load" parameterType="long" resultMap="FavoriteItemMap" >
        SELECT <include refid="cols_all" />
        FROM <include refid="tb" />
        WHERE id = #{id} LIMIT 1
    </select>

    <select id="loads" parameterType="list" resultMap="FavoriteItemMap" >
        SELECT <include refid="cols_all" />
        FROM <include refid="tb" />
        WHERE id IN
        <foreach item="id" collection="list" open="(" separator="," close=")">
            #{id}
        </foreach>
    </select>

    <select id="loadsBy" parameterType="map" resultMap="FavoriteItemMap" >
        SELECT <include refid="cols_all" />
        FROM <include refid="tb" />
        <include refid="criteria" />
        <if test="ids != null">
            AND id IN
            <foreach item="id" collection="ids" open="(" separator="," close=")">
                #{id}
            </foreach>
        </if>
    </select>

    <select id="list" parameterType="map" resultMap="FavoriteItemMap">
        SELECT
        <include refid="cols_all"/>
        FROM
        <include refid="tb"/>
        <include refid="criteria"/>
    </select>


    <delete id="delete" parameterType="long">
        DELETE FROM <include refid="tb"/>
        WHERE id = #{id}
    </delete>

    <delete id="deleteByItemIdAndUserId" parameterType="map">
        DELETE FROM <include refid="tb"/>
        WHERE item_id = #{itemId} AND user_id = #{userId}
    </delete>

    <select id="count" parameterType="map" resultType="long">
        SELECT COUNT(1)
        FROM <include refid="tb"/>
        <include refid="criteria"/>
    </select>
    <select id="paging" parameterType="map" resultMap="FavoriteItemMap">
        SELECT
        <include refid="cols_all"/>
        FROM <include refid="tb"/>
        <include refid="criteria"/>
        order by id desc
        LIMIT #{offset}, #{limit}
    </select>

</mapper>