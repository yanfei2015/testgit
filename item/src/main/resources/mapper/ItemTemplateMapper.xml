<?xml version="1.0" encoding="UTF-8" ?>
<!--
  ~ Copyright (c) 2014 杭州端点网络科技有限公司
  -->

<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >

<mapper namespace="ItemTemplate">
    <resultMap id="ItemTemplateMap" type="ItemTemplate">
        <id column="id" property="id"/>
        <result column="spu_id" property="spuId"/>
        <result column="name" property="name"/>
        <result column="main_image" property="mainImage"/>
        <result column="image1" property="image1"/>
        <result column="image2" property="image2"/>
        <result column="image3" property="image3"/>
        <result column="image4" property="image4"/>
        <result column="price" property="price"/>
        <result column="json_skus" property="jsonSkus"/>
        <result column="created_at" property="createdAt"/>
        <result column="updated_at" property="updatedAt"/>
    </resultMap>

    <sql id="tb">
        parana_item_templates
    </sql>

    <sql id="cols_all">
        id, <include refid="cols_exclude_id" />
    </sql>

    <sql id="cols_exclude_id">
        spu_id, name, main_image, image1, image2, image3, image4, price, json_skus, created_at, updated_at
    </sql>

    <sql id="vals">
        #{spuId}, #{name}, #{mainImage}, #{image1}, #{image2}, #{image3}, #{image4}, #{price}, #{jsonSkus}, now(), now()
    </sql>

    <sql id="criterias">
        WHERE 1 = 1
        <if test="spuId != null"> AND spu_id = #{spuId} </if>
    </sql>

    <insert id="create" parameterType="ItemTemplate" keyProperty="id" useGeneratedKeys="true">
        INSERT INTO
        <include refid="tb" />
        (<include refid="cols_exclude_id" />)
        VALUES
        (<include refid="vals" />)
    </insert>

    <select id="load" parameterType="long" resultMap="ItemTemplateMap" >
        SELECT <include refid="cols_all" />
        FROM <include refid="tb" />
        WHERE id = #{id} LIMIT 1
    </select>

    <select id="findBySpuId" parameterType="long" resultMap="ItemTemplateMap" >
        SELECT <include refid="cols_all" />
        FROM <include refid="tb" />
        WHERE spu_id = #{spuId} LIMIT 1
    </select>

    <select id="findBySpuIds" parameterType="list" resultMap="ItemTemplateMap" >
        SELECT <include refid="cols_all" />
        FROM <include refid="tb" />
        WHERE spu_id IN
        <foreach item="spuId" collection="list"
                 open="(" separator="," close=")">
            #{spuId}
        </foreach>
    </select>

    <select id="list" parameterType="ItemTemplate" resultMap="ItemTemplateMap">
        SELECT id,
        <include refid="cols_all"/>
        FROM
        <include refid="tb"/>
        <include refid="criterias"/>
    </select>

    <update id="update" parameterType="ItemTemplate">
        UPDATE <include refid="tb"/>
        <set>
            <if test="name != null"> name = #{name}, </if>
            <if test="spuId != null"> spu_id = #{spuId}, </if>
            <if test="mainImage != null"> main_image = #{mainImage},</if>
            <if test="image1 != null"> image1 = #{image1}, </if>
            <if test="image2 != null"> image2 = #{image2}, </if>
            <if test="image3 != null"> image3 = #{image3}, </if>
            <if test="image4 != null"> image4 = #{image4}, </if>
            <if test="price != null"> price = #{price}, </if>
            <if test="jsonSkus != null"> json_skus = #{jsonSkus}, </if>
            updated_at=now()
        </set>
        WHERE id=#{id}
    </update>

    <delete id="delete" parameterType="long">
        DELETE FROM <include refid="tb"/>
        WHERE id = #{id}
    </delete>

    <!-- 计数 -->
    <select id="count" parameterType="map" resultType="long">
        SELECT COUNT(1)
        FROM <include refid="tb"/>
        <include refid="criterias"/>
    </select>

    <!-- 分页查询 -->
    <select id="paging" parameterType="map" resultMap="ItemTemplateMap">
        SELECT
        <include refid="cols_all"/>
        FROM <include refid="tb"/>
        <include refid="criterias"/>
        LIMIT #{offset}, #{limit}
    </select>

</mapper>